# ── SAGE Agent Runtime Dependencies ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pillow>=10.0.0
rich>=13.7.0
python-multipart>=0.0.9
docker>=7.0.0

# ── SAGE Document Database Dependencies ──────────────────────────────────────
chromadb>=0.4.22
sentence-transformers>=2.2.2
pydantic>=2.0.0

# ── Document Parsing (Docling & Fallbacks) ───────────────────────────────────
# Kept for compatibility and fallback during migration
pymupdf>=1.24.0
python-docx>=1.1.0
# docling>=2.0.0  # Optional heavy document parser
